# Task Evidence Lab 开发约定

## 产品原则

- 结论必须能回指到证据，不能把“已填写”当成“已验证”。
- 记录事实、判断、风险和待确认项时保持明确区分。
- 本地 MVP 优先保证可追溯和可恢复，后续再引入账号、协作和自动化。
- 不伪造测试结果、来源、人员或交付状态。

## 当前技术边界

- 前端为 Vue 3 + Vite + TypeScript，后端为 FastAPI + SQLite。
- AI 能力默认关闭；评测必须先使用确定性规则。
- 导入内容只解析和展示，任何命令、脚本、路径、工具调用都不得执行。
- 这是单机本地应用，不承诺多人协作、认证、生产级审计或云端备份。

## 交付要求

- 修改后更新 README 或 `docs/mvp.md` 中受影响的说明。
- 报告验证结果时区分“本地验证”和“真实外部系统验证”。
- 数据库结构变更必须同步更新 `migrations/` 和后端初始化逻辑。
